# Ignore build output
/build/
/out/

# Ignore Gradle caches and generated files
.gradle/
/.gradle/
/build/

# ignore project files
.idea/
.vscode/
.env

# Keep Gradle wrapper files (do NOT ignore these!) Needed to keep Gradle wrapper scripts + jar in source control
# Also need to make an exception for the .jar files in build so that we can deploy on Render through Github
!gradlew
!gradlew.bat
!gradle/wrapper/gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties
#!build/libs/*.jar